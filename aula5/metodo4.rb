# Eu não preciso colocar o 'return'
# o último valor do método que será 
# retornado
def compare(a, b)
    a > b   
end 
    
a = 1
b = 2

# Ele vai retorna o boolean desse método
# que foi atribuido a variável
result = compare(a, b)
    
puts "O resultado da comparação é '#{result}'" 